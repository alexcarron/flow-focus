import dbUtils from '../modules/db-utils.js';
import { expect } from 'chai';

describe('cleanup_tables.sql', () => {
	it('SHOULD delete all tables', () => {
		// dbUtils.executeSQLFile('cleanup_tables.sql');
	});
});